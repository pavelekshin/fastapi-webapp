version: '3.7'

services:
  app_redis:
    image: redis:6.2-alpine
    hostname: redis
    container_name: redis
    command:
      - 'redis-server'
      - '--requirepass myStrongPassword'
    ports:
      - 36379:6379



networks:
  default:
    external: true
    name: app_main